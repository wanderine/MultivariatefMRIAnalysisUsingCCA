MultivariatefMRIAnalysisUsingCCA
================================

Matlab code for comparing the general linear model (GLM), canonical correlation analysis (CCA) and classification based analysis (CBA) using support vector machines (SVM).
